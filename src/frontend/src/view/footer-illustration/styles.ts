import styled from "styled-components";
import { BrokeManIcon } from "../../assets";

export const FooterIllustrationContainer = styled.div`
  display: flex;
  margin-top: auto;
  align-items: flex-end;
`;

export const IllustrationSvg = styled(BrokeManIcon)`
  position: relative;
  bottom: -65px;
  display: block;
  margin-left: auto;
  margin-right: auto;
`;
